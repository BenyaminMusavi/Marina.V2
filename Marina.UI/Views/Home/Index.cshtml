@{
    ViewData["Title"] = "Home Page";
}

@*<div class="col-xs-12 col-sm-6 col-lg-3 right">

    <div class="form-group rtl my-select">
        @Html.Label("کشور")
        @Html.DropDownList("Cntry_Code", await Marina.UI.General.Helper.GetRegionDropDown(), new { @class = "form-control", id = "Country" })
    </div>
</div>

*@
@*
<div class="form-outline mb-4">
    <label for="region" class="form-label">Region</label>
    @Html.DropDownListFor(model => 1,await Marina.UI.General.Helper.GetRegionDropDown() ,"select a region", new {style="height:40px;" , @class = "form-control", id="regionId" })
    <div class="invalid-feedback">
        Please enter a valid region.
    </div>
</div>*@